trigger:
- master

pool:
  vmImage: 'ubuntu-latest'


 

steps:
- script: |
    sudo docker login --username=$(dockerId) $(registry)
    sudo docker tag pomelobutter $(registry)/$(namespace)/$(image):dev
    sudo docker push $(registry)/$(namespace)/$(image):dev

  displayName: push to DockerHub